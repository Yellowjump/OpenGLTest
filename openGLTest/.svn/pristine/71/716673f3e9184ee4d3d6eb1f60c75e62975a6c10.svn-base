#version 330 core
out vec4 FragColor;

in vec3 ourColor;
in vec2 TexCoord;

// texture sampler
uniform sampler2D texture1;

void main()
{
    
    FragColor = vec4(ourColor,1.0f)*texture(texture1, vec2(TexCoord.x,TexCoord.y));
    //FragColor=vec4(0.3f,0.4f,0.0f, 1.0);
}
